# 2048 小游戏 - 微信小程序版

一款基于微信小程序开发的创新2048游戏，融合了角色技能系统，让经典玩法焕发新生。

## 📋 目录

- [项目简介](#项目简介)
- [核心功能](#核心功能)
- [技术栈](#技术栈)
- [项目结构](#项目结构)
- [快速开始](#快速开始)
- [游戏玩法](#游戏玩法)
- [角色系统](#角色系统)
- [开发指南](#开发指南)
- [配置说明](#配置说明)
- [更新日志](#更新日志)

## 🎮 项目简介

这是一款在经典2048游戏基础上创新的微信小程序游戏。游戏保留了2048的核心玩法，同时引入了三个独特的角色系统，每个角色拥有专属技能，为玩家提供不同的游戏策略和体验。

### 主要特色

- 🎯 **经典玩法**：保持原汁原味的2048游戏机制
- 🦸 **角色系统**：三个独特角色，各具特色技能
- ✨ **流畅动画**：精心设计的滑动、合并、特效动画
- 🔥 **紧急提示**：当空格≤3时触发红框警告
- 💾 **数据持久化**：自动保存历史最高分
- 📱 **响应式设计**：适配各种屏幕尺寸
- 🎨 **精美UI**：现代化的渐变色彩和玻璃态设计

## ⚡ 核心功能

### 1. 游戏初始化
- 创建4x4空网格作为游戏基本盘面
- 随机生成两个初始方块（90%概率为2，10%概率为4）
- 加载历史最高分记录

### 2. 方块操作
- **触摸滑动**：支持上下左右四个方向的滑动操作
- **方块移动**：相同数值方块合并，移动时紧密排列
- **智能生成**：每次操作后在空白格随机添加新方块

### 3. 分数系统
- **实时计分**：合并方块时累加分数
- **最高分记录**：自动保存并显示历史最高分
- **本地存储**：使用微信小程序存储API持久化数据

### 4. 游戏状态管理
- **结束检测**：无空格且无法合并时游戏结束
- **紧急状态**：空格≤3时触发红框脉冲效果
- **重新开始**：随时可开启新游戏（进行中需确认）

### 5. 角色技能系统
每个角色拥有独特的技能和获取机制，详见[角色系统](#角色系统)部分。

## 🛠 技术栈

- **框架**：微信小程序原生开发
- **语言**：JavaScript (ES6+)
- **样式**：WXSS (类CSS)
- **布局**：Flexbox
- **动画**：CSS3 Animations & Transitions
- **数据存储**：wx.setStorageSync / wx.getStorageSync

## 📁 项目结构

```
2048Game/
├── .cloudbase/              # 云开发配置
│   └── container/
│       └── debug.json
├── components/              # 组件目录
│   └── navigation-bar/      # 导航栏组件
│       ├── navigation-bar.js
│       ├── navigation-bar.json
│       ├── navigation-bar.wxml
│       └── navigation-bar.wxss
├── pages/                   # 页面目录
│   ├── index/              # 首页（角色选择）
│   │   ├── index.js
│   │   ├── index.json
│   │   ├── index.wxml
│   │   └── index.wxss
│   └── game/               # 游戏页面
│       ├── game.js         # 核心游戏逻辑
│       ├── game.json
│       ├── game.wxml
│       └── game.wxss
├── .eslintrc.js            # ESLint配置
├── .gitignore              # Git忽略文件
├── app.js                  # 小程序入口
├── app.json                # 全局配置
├── app.wxss                # 全局样式
├── sitemap.json            # 站点地图配置
└── README.md               # 项目文档
```

## 🚀 快速开始

### 环境要求

- 微信开发者工具
- Node.js (可选，用于ESLint)
- 微信小程序账号

### 安装步骤

1. **克隆项目**
   ```bash
   git clone [项目地址]
   cd 2048Game
   ```

2. **打开微信开发者工具**
   - 选择"导入项目"
   - 选择项目目录
   - 填写AppID（或使用测试号）

3. **运行项目**
   - 点击"编译"按钮
   - 在模拟器或真机中预览

### 开发调试

```bash
# 安装ESLint依赖（可选）
npm install

# 启动开发者工具进行调试
```

## 🎯 游戏玩法

### 基础规则

1. **移动方块**：在游戏面板上滑动手指
   - 向左滑：所有方块向左移动
   - 向右滑：所有方块向右移动
   - 向上滑：所有方块向上移动
   - 向下滑：所有方块向下移动

2. **合并规则**：
   - 相同数值的方块相遇时合并
   - 合并后数值翻倍
   - 得分 = 合并后的数值

3. **游戏目标**：
   - 不断合并方块获得更高分数
   - 挑战合成2048方块

4. **结束条件**：
   - 棋盘填满且无法移动
   - 或使用技能延续游戏

### 操作技巧

- **滑动距离**：超过30像素才触发移动
- **连续操作**：快速滑动可连续移动
- **策略布局**：将大数值方块集中在一个角落

## 🦸 角色系统

游戏提供三个独特角色，每个角色拥有专属技能和获取机制：

### 1️⃣ 时光回溯者 ⏳

**角色特点**：掌控时间与分数的回溯之力

**技能说明**：
- 可回到5步前的游戏状态
- 同时恢复对应的分数
- 初始拥有3次回溯机会

**获取机制**：
- 每获得2000分额外获得1次机会
- 自动保存最近5步的历史记录

**使用场景**：
- 失误操作后悔棋
- 游戏结束前的最后一搏
- 尝试不同的移动策略

**特殊机制**：
- 游戏结束时如有剩余次数，可选择回溯继续游戏

### 2️⃣ 方块消除者 🗑️

**角色特点**：操控棋盘方块的清除专家

**技能说明**：
- 消除棋盘中间4个方块
- 获得被消除方块数值总和的分数
- 消除后自动补充新方块

**获取机制**：
- 每获得1500分获得1次消除机会
- 初始无消除次数，需通过得分积累

**使用场景**：
- 中心区域方块堆积时清理空间
- 紧急状态下创造移动空间
- 战术性获取分数

**消除范围**：
```
□ □ □ □
□ ■ ■ □    ■ = 消除区域
□ ■ ■ □
□ □ □ □
```

### 3️⃣ 数值倍增师 ×2

**角色特点**：主宰方块数值的成长大师

**技能说明**：
- **指定翻倍**：选择任意方块翻倍其数值
- **全局翻倍**：将全场所有最低数值方块同时翻倍

**获取机制**：
- 每获得1000分获得1次指定翻倍机会
- 每获得3000分获得1次全局翻倍机会
- 两种翻倍独立计数

**使用场景**：
- **指定翻倍**：
  - 提升关键位置的高数值方块
  - 快速达成合并目标
  - 突破瓶颈状态
  
- **全局翻倍**：
  - 棋盘被低数值方块占据时
  - 整体提升棋盘数值水平
  - 批量清理低级方块

**操作方式**：
1. 点击技能按钮进入选择模式
2. 点击或滑动选择目标方块
3. 确认翻倍完成操作

**界面显示**：
- 左侧圆形按钮：指定翻倍次数
- 右侧"全"按钮：全局翻倍次数
- 选中方块有金色高亮效果

## 👨‍💻 开发指南

### 核心代码解析

#### 1. 游戏初始化
```javascript
initGame() {
  // 创建4x4空网格
  const board = Array(4).fill().map(() => Array(4).fill(0));
  
  // 初始化状态
  this.setData({
    board: board,
    score: 0,
    gameOver: false
  });
  
  // 添加两个初始方块
  this.addRandomTile();
  this.addRandomTile();
}
```

#### 2. 方块移动逻辑
```javascript
moveLeft(board, animationClasses) {
  let moved = false;
  let scoreAdded = 0;
  
  for (let row = 0; row < this.data.size; row++) {
    // 过滤空方块
    const line = board[row].filter(val => val !== 0);
    
    // 合并相同方块
    for (let i = 0; i < line.length - 1; i++) {
      if (line[i] === line[i + 1]) {
        line[i] *= 2;
        scoreAdded += line[i];
        line[i + 1] = 0;
      }
    }
    
    // 重新排列
    const newLine = line.filter(val => val !== 0);
    while (newLine.length < this.data.size) {
      newLine.push(0);
    }
    
    board[row] = newLine;
  }
  
  return { moved, scoreAdded };
}
```

#### 3. 技能系统实现
```javascript
// 时光回溯
timeRewindSkill() {
  const state = this.data.skillState.timeRewind;
  const history = state.history.pop();
  
  this.setData({
    board: history.board,
    score: history.score,
    'skillState.timeRewind.count': state.count - 1
  });
}

// 方块消除
squareEliminateSkill() {
  const board = this.data.board.map(row => [...row]);
  const mid = Math.floor(this.data.size / 2);
  
  for (let i = mid - 1; i <= mid; i++) {
    for (let j = mid - 1; j <= mid; j++) {
      board[i][j] = 0;
    }
  }
  
  this.setData({ board });
}
```

### 动画效果

#### CSS动画实现
```css
/* 合并效果 */
@keyframes merge {
  0% { transform: scale(1); }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); }
}

/* 滑动效果 */
@keyframes slideLeft {
  from { transform: translateX(40rpx); }
  to { transform: translateX(0); }
}

/* 紧急状态脉冲 */
@keyframes emergency-pulse {
  0%, 100% { 
    box-shadow: 0 0 40rpx rgba(255, 71, 87, 0.5); 
  }
  50% { 
    box-shadow: 0 0 60rpx rgba(255, 71, 87, 0.8); 
  }
}
```

### 数据存储

```javascript
// 保存最高分
saveBestScore() {
  if (this.data.score > this.data.bestScore) {
    wx.setStorageSync('2048-best', this.data.score);
  }
}

// 加载最高分
loadBestScore() {
  const bestScore = wx.getStorageSync('2048-best') || 0;
  this.setData({ bestScore });
}
```

## ⚙️ 配置说明

### app.json 配置
```json
{
  "pages": [
    "pages/index/index",
    "pages/game/game"
  ],
  "window": {
    "navigationBarTitleText": "2048小游戏",
    "navigationBarBackgroundColor": "#faf8ef",
    "navigationBarTextStyle": "black"
  }
}
```

### ESLint 配置
```javascript
module.exports = {
  env: {
    es6: true,
    browser: true,
    node: true
  },
  parserOptions: {
    ecmaVersion: 2018,
    sourceType: 'module'
  }
}
```

## 🎨 样式规范

### 颜色系统
```css
/* 主色调 */
--background: #faf8ef;
--tile-bg: #bbada0;
--text-dark: #776e65;
--text-light: #f9f6f2;

/* 方块颜色 */
tile-2: #eee4da
tile-4: #ede0c8
tile-8: #f2b179
tile-16: #f59563
tile-32: #f67c5f
tile-64: #f65e3b
tile-128: #edcf72
tile-256: #edcc61
tile-512: #edc850
tile-1024: #edc53f
tile-2048: #edc22e
```

### 响应式设计
```css
/* 适配小屏幕 */
@media (max-width: 375px) {
  .skill-controls {
    gap: 15rpx;
    padding: 15rpx 25rpx;
  }
  
  .grid-cell {
    width: 120rpx;
    height: 120rpx;
    font-size: 56rpx;
  }
}
```

## 📝 更新日志

### v1.0.0 (2024-12-26)
- ✨ 初始版本发布
- ✨ 实现基础2048游戏玩法
- ✨ 添加三个角色系统
- ✨ 实现时光回溯技能
- ✨ 实现方块消除技能
- ✨ 实现数值倍增技能
- ✨ 添加流畅的动画效果
- ✨ 实现紧急状态提示
- ✨ 实现数据持久化存储
- 🎨 优化UI界面设计
- 📱 适配不同屏幕尺寸

## 🤝 贡献指南

欢迎提交Issue和Pull Request！

### 开发流程
1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交改动 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交Pull Request

### 代码规范
- 遵循ESLint配置
- 保持代码注释清晰
- 变量命名语义化
- 函数功能单一化

## 📄 许可证

本项目采用 MIT 许可证

## 👥 作者

- 项目开发者：[您的名字]
- 联系方式：[您的邮箱]

## 🙏 致谢

- 感谢经典2048游戏提供的灵感
- 感谢微信小程序平台
- 感谢所有贡献者

## 📮 反馈与支持

如有问题或建议，请通过以下方式联系：

- 提交Issue
- 发送邮件
- 微信小程序内反馈

---

**祝您游戏愉快！🎮**
